import {
    Heading
} from 'styles/styled'

export default function MainPage(props: any) {
    
    return <div>
        <Heading style={{ textAlign: 'center' }}>Main Page</Heading>

        this is main page
    </div>
}